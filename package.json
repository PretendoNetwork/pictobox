{
  "name": "@pretendonetwork/pictobox",
  "version": "1.0.0",
  "description": "TypeScript library for interacting with several different image types used by the Wii U and 3DS",
  "scripts": {
    "lint": "npx eslint .",
    "build": "npm run lint && npm run clean && npx tsc && npx tsc-alias && npm run copy-files",
    "clean": "npx rimraf ./dist",
    "copy-files": "cp package.json dist/package.json && cp README.md dist/README.md"
  },
  "keywords": [],
  "author": "",
  "license": "APGL",
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "eslint": "^8.56.0",
    "rimraf": "^5.0.5",
    "tsc-alias": "^1.8.8"
  }
}
